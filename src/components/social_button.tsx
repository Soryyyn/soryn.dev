import React from "react";
import type { FontAwesomeIcon } from '@fortawesome/react-fontawesome'

export default function SocialButton({ faIconElement, link }: {
    faIconElement: typeof FontAwesomeIcon,
    link: string
}) {

}